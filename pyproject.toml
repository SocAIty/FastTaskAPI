[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "APIPod"
version = "1.0.26"
description="Create web-APIs for long-running tasks"
requires-python = ">=3.8"
authors = [
    { name = "SocAIty" }
]
license = {file = "LICENSE"}
readme = "README.md"
dependencies = [
    "uvicorn",
    "fastapi",
    'python-multipart>=0.0.20',
    "media-toolkit>=0.02.22",
    "socaity-schemas>=0.1.6",
    "socaity-cli>=0.1.8",
    "jinja2",
    "toml",
    "httpx",
]

[project.scripts]
apipod = "apipod.cli:main"

[project.optional-dependencies]
runpod = [
    "runpod>=1.7.7"
]
test = [
    "pytest",
    "httpx",
    "ruff",
    "mypy",
]


[project.urls]
Repository = "https://github.com/SocAIty/apipod"
Homepage = "https://www.socaity.ai"

[tool.pytest.ini_options]
testpaths = ["test"]
# Resolve in-repo apipod and sibling packages in local dev (CI installs from pip).
pythonpath = [".", "test", "../fastsdk", "../socaity-schemas", "../socaity-cli"]

[tool.ruff]
line-length = 120
exclude = ["venv", "dist", "build", "*.egg-info"]

[tool.ruff.lint]
# Mirror the previous flake8 config: ignore stylistic rules that conflict with
# black/our formatting conventions.
ignore = ["E203", "E261", "E501", "W293"]

[tool.mypy]
# Soft static typing: a warning signal, never a build gate (see CI).
ignore_missing_imports = true
follow_imports = "silent"
warn_unused_ignores = false
